<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Непрочитанное состояние (по умолчанию) -->
    <item android:state_selected="false" android:color="@color/accent" />
    <!-- Прочитанное состояние -->
    <item android:state_selected="true" android:color="@color/card_background" />
</selector> 